	# handlers	# handlers finais	# pacotes	# pacotes que correspondem a 90% of handlers finais		# handlers	# resignalers handlers	# resignaler handlers of external exceptions	# resignaler handlers of external exceptions which signal project exceptions		# signalers	# resignalers	# resignalers of unchecked exceptions		# project exceptions	# signalers of project exception	# exceptions which signal 90% of signalers		# project exceptions	# subtypes of most subtyped project exception		# handlers	# final handlers	# final handlers with logging actions		# handlers	# handlers with calls to dedicated handlers	# calls to dedicated handlers	# classes que correspondem a 90% das chamadas a dedicated handlers		
[161920] Samuel-Oliveira-Java_NFe	